/* Twig function returning an asset's filename (no Vite manifest lookup, so it's a passthrough). */
// eslint-disable-next-line no-unused-vars
export default function getAssetPath(assetName, directory = null) {
  return assetName;
}
